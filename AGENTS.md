# Repository Routing

`admin-panel` owns the Next.js admin and public web applications. Workspace
routing lives in `/Users/serhiimytakii/Projects/Levantem/AGENTS.md`.

## Read First

- Execution, validation, and documentation workflow:
  `docs/agents/execution-routing.md`.
- Public, admin, deeplink, content, HTTP, and SEO boundaries:
  `docs/ADMIN_PANEL_ARCHITECTURE_CORE.md`.
- Feature-specific behavior: the relevant document under `docs/`.
- Commands and quality configuration: `package.json`, `.eslintrc.json`,
  `tsconfig.json`, and `jest.config.js`.

## Local Constraint

Do not introduce new explicit `any`.
